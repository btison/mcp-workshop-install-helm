{{- if .Values.quay.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.quay.applicationName }}
  namespace: {{ .Values.quay.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.quay.argocd.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: {{ .Values.quay.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ .Values.quay.source.repoUrl }}
    targetRevision: {{ .Values.quay.source.targetRevision }}
    path: {{ .Values.quay.source.path }}
    helm:
      valuesObject:
        {{ toYaml .Values.quay.source.helm.values | nindent 8 }}
{{- end }}
