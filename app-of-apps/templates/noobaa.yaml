{{- if .Values.noobaa.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.noobaa.applicationName }}
  namespace: {{ .Values.noobaa.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.noobaa.argocd.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: {{ .Values.noobaa.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ .Values.noobaa.source.repoUrl }}
    targetRevision: {{ .Values.noobaa.source.targetRevision }}
    path: {{ .Values.noobaa.source.path }}
    helm:
      valuesObject:
        {{ toYaml .Values.noobaa.source.helm.values | nindent 8 }}
{{- end }}
