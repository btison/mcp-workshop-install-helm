{{- if .Values.globexComplaintAgent.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.globexComplaintAgent.applicationName }}
  namespace: {{ .Values.globexComplaintAgent.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.globexComplaintAgent.argocd.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: {{ .Values.globexComplaintAgent.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ .Values.globexComplaintAgent.source.repoUrl }}
    targetRevision: {{ .Values.globexComplaintAgent.source.targetRevision }}
    path: {{ .Values.globexComplaintAgent.source.path }}
    helm:
      valuesObject:
        {{ toYaml .Values.globexComplaintAgent.source.helm.values | nindent 8 }}
{{- end }}