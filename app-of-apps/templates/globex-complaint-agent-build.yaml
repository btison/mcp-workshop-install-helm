{{- if .Values.globexComplaintAgentBuild.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.globexComplaintAgentBuild.applicationName }}
  namespace: {{ .Values.globexComplaintAgentBuild.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.globexComplaintAgentBuild.argocd.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: {{ .Values.globexComplaintAgentBuild.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ .Values.globexComplaintAgentBuild.source.repoUrl }}
    targetRevision: {{ .Values.globexComplaintAgentBuild.source.targetRevision }}
    path: {{ .Values.globexComplaintAgentBuild.source.path }}
    helm:
      valuesObject:
        {{ toYaml .Values.globexComplaintAgentBuild.source.helm.values | nindent 8 }}
{{- end }}