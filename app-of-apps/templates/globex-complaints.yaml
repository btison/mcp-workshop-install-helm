{{- if .Values.globexComplaints.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.globexComplaints.applicationName }}
  namespace: {{ .Values.globexComplaints.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.globexComplaints.argocd.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: {{ .Values.globexComplaints.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ .Values.globexComplaints.source.repoUrl }}
    targetRevision: {{ .Values.globexComplaints.source.targetRevision }}
    path: {{ .Values.globexComplaints.source.path }}
    helm:
      valuesObject:
        {{ toYaml .Values.globexComplaints.source.helm.values | nindent 8 }}
{{- end }}