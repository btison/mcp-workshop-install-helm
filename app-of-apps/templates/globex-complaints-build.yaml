{{- if .Values.globexComplaintsBuild.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.globexComplaintsBuild.applicationName }}
  namespace: {{ .Values.globexComplaintsBuild.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.globexComplaintsBuild.argocd.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: {{ .Values.globexComplaintsBuild.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ .Values.globexComplaintsBuild.source.repoUrl }}
    targetRevision: {{ .Values.globexComplaintsBuild.source.targetRevision }}
    path: {{ .Values.globexComplaintsBuild.source.path }}
    helm:
      valuesObject:
        {{ toYaml .Values.globexComplaintsBuild.source.helm.values | nindent 8 }}
{{- end }}