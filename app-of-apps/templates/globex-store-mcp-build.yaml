{{- if .Values.globexStoreMcpBuild.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.globexStoreMcpBuild.applicationName }}
  namespace: {{ .Values.globexStoreMcpBuild.applicationNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.globexStoreMcpBuild.argocd.syncwave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/foreground
spec:
  destination:
    name: ""
    namespace: {{ .Values.globexStoreMcpBuild.destinationNamespace }}
    server: 'https://kubernetes.default.svc'
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 10
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  source:
    repoURL: {{ .Values.globexStoreMcpBuild.source.repoUrl }}
    targetRevision: {{ .Values.globexStoreMcpBuild.source.targetRevision }}
    path: {{ .Values.globexStoreMcpBuild.source.path }}
    helm:
      valuesObject:
        {{ toYaml .Values.globexStoreMcpBuild.source.helm.values | nindent 8 }}
{{- end }}